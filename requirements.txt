flask>=3.0.0
gspread>=6.0.0
google-auth>=2.0.0
gunicorn>=21.0.0
openpyxl>=3.1.0
psycopg[binary,pool]>=3.2.0
python-dotenv>=1.0.0
Pillow>=10.0.0  # 2026-07-14: 관리팀 식수 집계 이미지(PNG) 저장용(폰에서 엑셀보다 편하게)
# v2: 모바일 서버사이드 얼굴인식(LBPH) 폐기 → opencv/numpy 제거(Render 메모리·빌드 경량화).
#     얼굴 인증은 PC 키오스크 전용(ArcFace).
